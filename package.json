{
  "name": "mdg-bp-duplicate-checker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MDG BP duplicate checker: private transactional precomputed index with fail-closed legacy Sheets compatibility.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check _lib/duplicate.js && node --check api/index.js && node --check api/health.js && node --check api/check.js && node --check _lib/keyed-sheets.js",
    "test": "node --test tests/*.mjs",
    "smoke": "node --test tests/render_startup.test.js"
  },
  "packageManager": "yarn@1.22.22"
}
